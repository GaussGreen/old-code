#ifndef NAGC06
#define NAGC06
#ifdef __cplusplus
extern "C"
{
#endif


/* <nagc06.h>
 *
 * Copyright 1996 Numerical Algorithms Group
 *
 * Include file for NAG C Library c06 Chapter
 *
 * Mark 4, revised, 1996.
 * Mark 5 revised. IER-2143 (Feb 1998).
 */

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06eac(Integer pts, double NAG_HUGE x[], NagError NAG_HUGE *fail);
#else
extern void c06eac();
#endif

#ifdef NAG_PROTO
extern void c06eaq(double NAG_HUGE *x, Integer pts, Integer m, Integer p);
#else
extern void c06eaq();
#endif

#ifdef NAG_PROTO
extern void c06ear(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, double NAG_HUGE *x3, Integer m3, double NAG_HUGE *x4, Integer m4,
            double NAG_HUGE *x5, Integer m5, double NAG_HUGE *x6, Integer m6, double NAG_HUGE *x7,
            Integer m7, Integer m);
#else
extern void c06ear();
#endif

#ifdef NAG_PROTO
extern void c06eas(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2, Integer m2,
            double NAG_HUGE *x3, Integer m3, double NAG_HUGE *x4, Integer m4, Integer m);
#else
extern void c06eas();
#endif

#ifdef NAG_PROTO
extern void c06eat(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, double NAG_HUGE *x3, Integer m3, Integer m);
#else
extern void c06eat();
#endif

#ifdef NAG_PROTO
extern void c06eau(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, Integer m);
#else
extern void c06eau();
#endif

#ifdef NAG_PROTO
extern void c06eav(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, Integer m);
#else
extern void c06eav();
#endif

#ifdef NAG_PROTO
extern void c06eaw(double NAG_HUGE *x, Integer pts, Integer NAG_HUGE *factor);
#else
extern void c06eaw();
#endif

#ifdef NAG_PROTO
extern void c06eax(double NAG_HUGE *x, Integer pts, Integer NAG_HUGE *sym);
#else
extern void c06eax();
#endif

#ifdef NAG_PROTO
extern void c06eay(double NAG_HUGE *x, Integer pts, Integer NAG_HUGE *sym, Integer psym, Integer NAG_HUGE *unsym);
#else
extern void c06eay();
#endif

#ifdef NAG_PROTO
extern void c06eaz(Integer pts, Integer pmax, Integer twogrp, Integer NAG_HUGE *factor, Integer NAG_HUGE *sym,
            Integer NAG_HUGE *psym, Integer NAG_HUGE *unsym, NagError NAG_HUGE *error);
#else
extern void c06eaz();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06ebc(Integer pts, double NAG_HUGE x[], NagError NAG_HUGE *fail);
#else
extern void c06ebc();
#endif

#ifdef NAG_PROTO
extern void c06ebq(double NAG_HUGE *x, Integer pts, Integer m, Integer p);
#else
extern void c06ebq();
#endif

#ifdef NAG_PROTO
extern void c06ebr(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2, Integer m2,
            double NAG_HUGE *x3, Integer m3, double NAG_HUGE *x4, Integer m4, double NAG_HUGE *x5, Integer m5,
            double NAG_HUGE *x6, Integer m6, double NAG_HUGE *x7, Integer m7, Integer m);
#else
extern void c06ebr();
#endif

#ifdef NAG_PROTO
extern void c06ebs(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, double NAG_HUGE *x3, Integer m3, double NAG_HUGE *x4, Integer m4,
            Integer m);
#else
extern void c06ebs();
#endif

#ifdef NAG_PROTO
extern void c06ebt(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, double NAG_HUGE *x3, Integer m3, Integer m);
#else
extern void c06ebt();
#endif

#ifdef NAG_PROTO
extern void c06ebu(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, double NAG_HUGE *x2,
            Integer m2, Integer m);
#else
extern void c06ebu();
#endif

#ifdef NAG_PROTO
extern void c06ebv(double NAG_HUGE *x0, Integer pts, double NAG_HUGE *x1, Integer m1, Integer m);
#else
extern void c06ebv();
#endif

#ifdef NAG_PROTO
extern void c06ebw(double NAG_HUGE *x, Integer pts, Integer NAG_HUGE *factor);
#else
extern void c06ebw();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06ecc(Integer pts, double NAG_HUGE x[], double NAG_HUGE y[], NagError NAG_HUGE *fail);
#else
extern void c06ecc();
#endif

#ifdef NAG_PROTO
extern void c06ecq(double NAG_HUGE *x, double NAG_HUGE *y, Integer pts, Integer m, Integer p);
#else
extern void c06ecq();
#endif

#ifdef NAG_PROTO
extern void c06ecr(double NAG_HUGE *x0, double NAG_HUGE *y0, Integer pts, double NAG_HUGE *x1, double NAG_HUGE *y1,
            Integer m1, double NAG_HUGE *x2, double NAG_HUGE *y2, Integer m2, double NAG_HUGE *x3,
            double NAG_HUGE *y3, Integer m3, double NAG_HUGE *x4, double NAG_HUGE *y4, Integer m4,
            double NAG_HUGE *x5, double NAG_HUGE *y5, Integer m5, double NAG_HUGE *x6, double NAG_HUGE *y6,
            Integer m6, double NAG_HUGE *x7, double NAG_HUGE *y7, Integer m7, Integer m);
#else
extern void c06ecr();
#endif

#ifdef NAG_PROTO
extern void c06ecs(double NAG_HUGE *x0, double NAG_HUGE *y0, Integer pts, double NAG_HUGE *x1, double NAG_HUGE *y1,
            Integer m1, double NAG_HUGE *x2, double NAG_HUGE *y2, Integer m2, double NAG_HUGE *x3,
            double NAG_HUGE *y3, Integer m3, double NAG_HUGE *x4, double NAG_HUGE *y4, Integer m4,
            Integer m);
#else
extern void c06ecs();
#endif

#ifdef NAG_PROTO
extern void c06ect(double NAG_HUGE *x0, double NAG_HUGE *y0, Integer pts, double NAG_HUGE *x1, double NAG_HUGE *y1,
            Integer m1, double NAG_HUGE *x2, double NAG_HUGE *y2, Integer m2, double NAG_HUGE *x3,
            double NAG_HUGE *y3, Integer m3, Integer m);
#else
extern void c06ect();
#endif

#ifdef NAG_PROTO
extern void c06ecu(double NAG_HUGE *x0, double NAG_HUGE *y0, Integer pts, double NAG_HUGE *x1, double NAG_HUGE *y1, Integer m1,
            double NAG_HUGE *x2, double NAG_HUGE *y2, Integer m2, Integer m);
#else
extern void c06ecu();
#endif

#ifdef NAG_PROTO
extern void c06ecv(double NAG_HUGE *x0, double NAG_HUGE *y0, Integer pts, double NAG_HUGE *x1, double NAG_HUGE *y1,
            Integer m1, Integer m);
#else
extern void c06ecv();
#endif

#ifdef NAG_PROTO
extern void c06ecw(double NAG_HUGE *x, double NAG_HUGE *y, Integer pts, Integer NAG_HUGE *factor);
#else
extern void c06ecw();
#endif

#ifdef NAG_PROTO
extern void c06ecx(double NAG_HUGE *x, double NAG_HUGE *y, Integer pts, Integer NAG_HUGE *sym);
#else
extern void c06ecx();
#endif

#ifdef NAG_PROTO
extern void c06ecy(double NAG_HUGE *x, double NAG_HUGE *y, Integer pts, Integer NAG_HUGE *sym, Integer psym,
            Integer NAG_HUGE *unsym);
#else
extern void c06ecy();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06ekc(Nag_VectorOp Operation, Integer n, double NAG_HUGE x[], double NAG_HUGE y[], NagError NAG_HUGE *fail);
#else
extern void c06ekc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06fpc(Integer m, Integer n, double NAG_HUGE x[], double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06fpc();
#endif

#ifdef NAG_PROTO
extern void c06fpr(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer q, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fpr();
#endif

#ifdef NAG_PROTO
extern void c06fps(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r, double NAG_HUGE *cosine,
            double NAG_HUGE *sine);
#else
extern void c06fps();
#endif

#ifdef NAG_PROTO
extern void c06fpt(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r, double NAG_HUGE *cosine,
            double NAG_HUGE *sine);
#else
extern void c06fpt();
#endif

#ifdef NAG_PROTO
extern void c06fpu(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r, double NAG_HUGE *cosine,
            double NAG_HUGE *sine);
#else
extern void c06fpu();
#endif

#ifdef NAG_PROTO
extern void c06fpv(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r, double NAG_HUGE *cosine,
            double NAG_HUGE *sine);
#else
extern void c06fpv();
#endif

#ifdef NAG_PROTO
extern void c06fpw(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r, double NAG_HUGE *cosine,
            double NAG_HUGE *sine);
#else
extern void c06fpw();
#endif

#ifdef NAG_PROTO
extern void c06fpx(double NAG_HUGE *a, Integer m, Integer n, Integer NAG_HUGE *q, Integer nq,
            double NAG_HUGE *trig, int NAG_HUGE *localerror);
#else
extern void c06fpx();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06fpz(Integer n, Integer NAG_HUGE *nq, Integer NAG_HUGE *q);
#else
extern void c06fpz();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06fqc(Integer m, Integer n, double NAG_HUGE x[], double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06fqc();
#endif

#ifdef NAG_PROTO
extern void c06fqq(double NAG_HUGE *a, Integer m, Integer n);
#else
extern void c06fqq();
#endif

#ifdef NAG_PROTO
extern void c06fqr(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer q, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqr();
#endif

#ifdef NAG_PROTO
extern void c06fqs(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqs();
#endif

#ifdef NAG_PROTO
extern void c06fqt(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqt();
#endif

#ifdef NAG_PROTO
extern void c06fqu(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqu();
#endif

#ifdef NAG_PROTO
extern void c06fqv(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqv();
#endif

#ifdef NAG_PROTO
extern void c06fqw(double NAG_HUGE *a, double NAG_HUGE *b, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fqw();
#endif

#ifdef NAG_PROTO
extern void c06fqx(double NAG_HUGE *a, Integer m, Integer n, Integer NAG_HUGE *q, Integer nq,
            double NAG_HUGE *trig, int NAG_HUGE *localerror);
#else
extern void c06fqx();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06frc(Integer m, Integer n, double NAG_HUGE x[], double NAG_HUGE y[], double NAG_HUGE trig[],
            NagError NAG_HUGE *fail);
#else
extern void c06frc();
#endif

#ifdef NAG_PROTO
extern void c06frr(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer q,
            Integer r, double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06frr();
#endif

#ifdef NAG_PROTO
extern void c06frs(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06frs();
#endif

#ifdef NAG_PROTO
extern void c06frt(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06frt();
#endif

#ifdef NAG_PROTO
extern void c06fru(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06fru();
#endif

#ifdef NAG_PROTO
extern void c06frv(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06frv();
#endif

#ifdef NAG_PROTO
extern void c06frw(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *br, double NAG_HUGE *bi, Integer p, Integer r,
            double NAG_HUGE *cosine, double NAG_HUGE *sine);
#else
extern void c06frw();
#endif

#ifdef NAG_PROTO
extern void c06frx(double NAG_HUGE *x, double NAG_HUGE *y, Integer m, Integer n, Integer NAG_HUGE *q, Integer nq,
            double NAG_HUGE *trig, int NAG_HUGE *localerror);
#else
extern void c06frx();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06fuc(Integer m, Integer n, double NAG_HUGE x[], double NAG_HUGE y[], double NAG_HUGE trigm[],
            double NAG_HUGE trign[], NagError NAG_HUGE *fail);
#else
extern void c06fuc();
#endif

#ifdef NAG_PROTO
extern void c06fuz(double NAG_HUGE *x, double NAG_HUGE *y, double NAG_HUGE *xx, double NAG_HUGE *yy, Integer m, Integer n);
#else
extern void c06fuz();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06gbc(Integer n, double NAG_HUGE x[], NagError NAG_HUGE *fail);
#else
extern void c06gbc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06gcc(Integer n, double NAG_HUGE y[], NagError NAG_HUGE *fail);
#else
extern void c06gcc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06gqc(Integer m, Integer n, double NAG_HUGE x[], NagError NAG_HUGE *fail);
#else
extern void c06gqc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06gsc(Integer m, Integer n, double NAG_HUGE x[], double NAG_HUGE u[], double NAG_HUGE v[],
            NagError NAG_HUGE *fail);
#else
extern void c06gsc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06gzc(Integer n, double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06gzc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06hac(Integer m, Integer n, double NAG_HUGE x[],
            double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06hac();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06hbc(Integer m, Integer n, double NAG_HUGE x[],
            double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06hbc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06hcc(Nag_TransformDirection direct, Integer m, Integer n, double NAG_HUGE x[],
            double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06hcc();
#endif

#ifdef NAG_PROTO
extern NAG_DLL_EXPIMP void NAG_CALL c06hdc(Nag_TransformDirection direct, Integer m, Integer n, double NAG_HUGE x[],
            double NAG_HUGE trig[], NagError NAG_HUGE *fail);
#else
extern void c06hdc();
#endif


    /* The following defines are for NAG stringent test programs only. */

#define NUMFAC 20   /* Limit on number of prime factors in n, used in c06eac,    */
		    /* c06ebc, c06ecc, c06ekc. To test error exits in stringents */
		    /* NUMFAC may need to be set to a value of 9 or less as      */
		    /* array sizes to test n with 20 or more factors may be      */
		    /* too large for compiler.  				 */
		    /* (n with 20 factors is at least 1048576)			 */
#define C06_STRINGENT_SIZE 1024  /* Array size for stringent programs c06eac,    */
				 /* c06ecc, c06ekc. Change for values of n       */
				 /* larger than 1024. If set lower than 1024     */
				 /* amend data file to remove values of n higher */
				 /* than C06_STRINGENT_SIZE.                     */

#ifdef __cplusplus
}
#endif
#endif /* not NAGC06 */
